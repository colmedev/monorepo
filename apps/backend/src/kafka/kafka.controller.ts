// Kafka controller removed per user request. Implement later when ready.
import { Controller } from '@nestjs/common';

@Controller('/kafka')
export class KafkaController {
  // Placeholder controller: endpoints disabled until Kafka is re-added.
}
